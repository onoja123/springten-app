import { View, Text } from "react-native";
import React from "react";

const CustomSpinner = () => {
  return (
    <View>
      <Text>CustomSpinner</Text>
    </View>
  );
};

export default CustomSpinner;
